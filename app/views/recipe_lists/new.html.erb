<% if @recipe_list.errors.any? %>
  <% @recipe_list.errors.each do |attr, msg| %>
    <%= msg %>
  <% end %>
<% end %>

<%= form_for @recipe_list, url: lists_path do |f| %>
  <%= f.label :name, 'Nome' %>:
  <%= f.text_field :name %>

  <%= f.submit 'Enviar' %>

<% end %>